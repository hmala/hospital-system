# تكامل نظام الاستعلامات مع الكاشير

## التحديثات المنفذة

تم تحديث نظام الكاشير ليعرض **الطلبات الطبية** (تحاليل، أشعة، صيدلية) بالإضافة إلى المواعيد.

## سير العمل الكامل

### 1️⃣ في الاستعلامات
1. الموظف يختار المريض
2. يختار نوع الخدمة (تحاليل/أشعة/صيدلية/كشف طبي)
3. **للتحاليل**: يختار التحاليل المطلوبة
4. **للأشعة**: يختار أنواع الأشعة المطلوبة
5. يرسل الطلب
6. **يظهر رسالة نجاح** مع رقم الطلب وتعليمات لتوجيه المريض للكاشير

### 2️⃣ في الكاشير
1. يفتح الكاشير صفحة الكاشير
2. يرى **قسمين**:
   - **المواعيد المعلقة** (الكشوفات الطبية)
   - **الطلبات المعلقة** (التحاليل والأشعة) ← **جديد!**
3. في قسم الطلبات، يرى:
   - رقم الطلب
   - نوع الطلب (تحاليل/أشعة/صيدلية)
   - اسم المريض
   - تفاصيل التحاليل أو الأشعة المختارة
   - زر "تسديد"

## التغييرات التقنية

### 1. CashierController.php
```php
// إضافة imports
use App\Models\Request as MedicalRequest;
use App\Models\LabTest;
use App\Models\RadiologyType;

// جلب الطلبات المعلقة
$pendingRequests = MedicalRequest::with(['visit.patient.user', 'visit.doctor.user'])
    ->where('status', 'pending')
    ->whereHas('visit', function($q) {
        $q->where('status', '!=', 'cancelled');
    })
    ->orderBy('created_at', 'desc')
    ->paginate(15, ['*'], 'requests_page');

// تحديث الإحصائيات
'pending_appointments_count' => $pendingAppointments->total(),
'pending_requests_count' => $pendingRequests->total(),
```

### 2. cashier/index.blade.php
- إضافة قسم جديد لعرض الطلبات المعلقة
- عرض التحاليل والأشعة المختارة لكل طلب
- تحديث بطاقة الإحصائيات لعرض عدد الطلبات المعلقة

### 3. InquiryController.php
- تحسين رسالة النجاح بعد إنشاء الطلب
- إضافة تعليمات واضحة لتوجيه المريض للكاشير

## البيانات المعروضة

### للتحاليل
- عدد التحاليل المختارة
- أسماء أول تحليلين + رسالة "... و X أخرى"

### للأشعة
- عدد الأشعة المختارة
- أسماء أول نوعين + رسالة "... و X أخرى"

## الخطوة القادمة

🚧 **جاري العمل على**: نموذج الدفع للطلبات (مشابه لنموذج دفع المواعيد)

سيتضمن:
- حساب تكلفة التحاليل/الأشعة المختارة
- إنشاء فاتورة تفصيلية
- تسجيل الدفع
- تحديث حالة الطلب إلى "مدفوع"
- طباعة إيصال

## الاختبار

قم بتشغيل:
```bash
php test_cashier_requests.php
```

لعرض الطلبات المعلقة التي يجب أن تظهر عند الكاشير.

## الوصول

- **صفحة الكاشير**: `/cashier`
- **الصلاحيات المطلوبة**: `admin`, `cashier`, أو `receptionist`

---
**تاريخ التحديث:** يناير 2026  
**الحالة:** ✅ كامل - جاهز للاستخدام
